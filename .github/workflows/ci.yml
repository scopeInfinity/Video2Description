name: CI

on:
  push:
    branches:
    - VideoCaption
    - actions
    - speedo
 
  pull_request:
    branches:
    - VideoCaption 
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [2.7]

    steps:
      - uses: actions/checkout@v2

      - uses: docker://scopeinfinity/video2description:frontend
      - uses: docker://scopeinfinity/video2description:deploy
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          bash early_tests.sh
          sudo apt-get install firefox
          wget 'https://github.com/mozilla/geckodriver/releases/download/v0.26.0/geckodriver-v0.26.0-linux64.tar.gz'
          tar -xvzf geckodriver-v0.26.0-linux64.tar.gz
          sudo mv geckodriver /usr/local/bin

      - name: Build
        run: |
          docker-compose build
          
      - name: Test
        run: |
          bash docker_test.sh

      # Push image to docker on 'push' event only.
      - name: Deploy to Docker Hub
        if: ${{ github.event_name == 'push' }}
        env:
          DOCKER_USERNAME: "scopeinfinity"
          DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        run: |
          { echo "$DOCKER_TOKEN" | docker login -u "$DOCKER_USERNAME" --password-stdin ; } && bash docker_push.sh
